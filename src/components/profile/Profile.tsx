import { Container } from "@mui/material";
import React from "react";

export const Profile = () => {
    return (
        <section>
            <Container maxWidth="lg">
                <h1>Profile</h1>
            </Container>
        </section>
    );
};
